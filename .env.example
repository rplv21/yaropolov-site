# Скопировать в .env (или задать в Vercel → Settings → Environment Variables)
# Токен и chat_id никогда не должны попадать в репозиторий.

# Токен бота @YaropolovPbot, выдаётся BotFather
TELEGRAM_BOT_TOKEN=

# ID чата/канала, куда бот отправляет заявки с формы
TELEGRAM_CHAT_ID=
